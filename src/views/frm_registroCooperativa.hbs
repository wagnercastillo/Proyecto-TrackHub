
    {{>navigation_admin1}}
  
  
    {{>nueva_cooperativa}}

<div class="row">
    <div class="col-md-3">
    </div>
    <col-md-8>
        <table class="table">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Direccion</th>
                    <th>Operaciones</th>
                </tr>
            </thead>
            <tbody>
                {{#each cooperativas}}
                <tr>
                    <td>{{nombre}}</td>
                    <td>{{direccion}}</td>
                    <td>
                        <a href="{{imageURL}}" target="_black" class="btn btn-primary">Vista</a>
                        <a class="btn btn-danger">Inabilitar</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </col-md-8>
</div>